<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:dropDownListViewStyle">@style/SpinnerDropDownStyle</item>
    </style>

    <style name="SpinnerDropDownStyle" parent="android:style/Widget.ListView.DropDown">
        <item name="android:divider">@color/divider</item>
        <item name="android:dividerHeight">@dimen/line_size</item>
    </style>

    <!-- 默认使用,所以必须拷贝此 style  ,这个必须要,默认效果,当然如果你拷贝源码修改了也是可以不要的...  -->
    <style name="JDialogStyle" parent="@android:style/Theme.Dialog">
        <!-- 背景透明 -->
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <!-- 浮于Activity之上 -->
        <item name="android:windowIsFloating">true</item>
        <!-- 边框 -->
        <item name="android:windowFrame">@null</item>
        <!-- Dialog以外的区域模糊效果 -->
        <item name="android:backgroundDimEnabled">true</item>
        <!-- 决定背景透明度 -->
        <item name="android:backgroundDimAmount">0.3</item>
        <!-- 无标题 -->
        <item name="android:windowNoTitle">true</item>
        <!-- 半透明 -->
        <item name="android:windowIsTranslucent">true</item>
        <!-- Dialog进入及退出动画,这样所有使用此 style 都有这个动画, 可以单独指定的 -->
        <!--<item name="android:windowAnimationStyle">@style/JDialogAnimation</item>-->
    </style>


    <style name="navigationActiveTextStyle">
        <item name="android:textSize">@dimen/design_bottom_navigation_active_text_size</item>
    </style>

    <style name="navigationInactiveTextStyle">
        <item name="android:textSize">@dimen/design_bottom_navigation_inactive_text_size</item>
    </style>


    <style name="MyTabLayoutTextAppearance" parent="TextAppearance.AppCompat.Widget.ActionBar.Title">
        <item name="android:textSize">11sp</item>
    </style>


</resources>
